#include <stdio.h>

int main(int argc, char* argv[]){
    int a,b;
    printf("Type 2 numbers: ");
    scanf("%d %d", a,b);
    printf("The addition of these numbers is: %d", (a + b));
    return 0;
}